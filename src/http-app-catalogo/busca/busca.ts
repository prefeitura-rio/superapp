/**
 * Generated by orval v8.6.2 🍺
 * Do not edit manually.
 * app-catalogo
 * Discovery layer unificada da Prefeitura do Rio de Janeiro. Indexa serviços públicos, cursos, vagas e oportunidades MEI com busca full-text e recomendação personalizada por perfil de cidadão.
 * OpenAPI spec version: 1.0
 */
import type {
  GetApiPublicSearch500,
  GetApiPublicSearchParams,
  GetApiV1Search500,
  GetApiV1SearchParams,
  ModelsSearchResponse,
} from '../models'

import { customFetchAppCatalogo } from '../../../custom-fetch-app-catalogo'

/**
 * Busca por serviços, cursos, vagas e oportunidades MEI. Suporta sintaxe websearch_to_tsquery: aspas para frase exata, - para exclusão, OR para alternativas.
 * @summary Busca no catálogo
 */
export type getApiPublicSearchResponse200 = {
  data: ModelsSearchResponse
  status: 200
}

export type getApiPublicSearchResponse500 = {
  data: GetApiPublicSearch500
  status: 500
}

export type getApiPublicSearchResponseSuccess =
  getApiPublicSearchResponse200 & {
    headers: Headers
  }
export type getApiPublicSearchResponseError = getApiPublicSearchResponse500 & {
  headers: Headers
}

export type getApiPublicSearchResponse =
  | getApiPublicSearchResponseSuccess
  | getApiPublicSearchResponseError

export const getGetApiPublicSearchUrl = (params?: GetApiPublicSearchParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ['types']

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach(v => {
        normalizedParams.append(key, v === null ? 'null' : v.toString())
      })
      return
    }

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/public/search?${stringifiedParams}`
    : `/api/public/search`
}

export const getApiPublicSearch = async (
  params?: GetApiPublicSearchParams,
  options?: RequestInit
): Promise<getApiPublicSearchResponse> => {
  return customFetchAppCatalogo<getApiPublicSearchResponse>(
    getGetApiPublicSearchUrl(params),
    {
      ...options,
      method: 'GET',
    }
  )
}

/**
 * Busca por serviços, cursos, vagas e oportunidades MEI. Suporta sintaxe websearch_to_tsquery: aspas para frase exata, - para exclusão, OR para alternativas.
 * @summary Busca no catálogo
 */
export type getApiV1SearchResponse200 = {
  data: ModelsSearchResponse
  status: 200
}

export type getApiV1SearchResponse500 = {
  data: GetApiV1Search500
  status: 500
}

export type getApiV1SearchResponseSuccess = getApiV1SearchResponse200 & {
  headers: Headers
}
export type getApiV1SearchResponseError = getApiV1SearchResponse500 & {
  headers: Headers
}

export type getApiV1SearchResponse =
  | getApiV1SearchResponseSuccess
  | getApiV1SearchResponseError

export const getGetApiV1SearchUrl = (params?: GetApiV1SearchParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ['types']

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach(v => {
        normalizedParams.append(key, v === null ? 'null' : v.toString())
      })
      return
    }

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/v1/search?${stringifiedParams}`
    : `/api/v1/search`
}

export const getApiV1Search = async (
  params?: GetApiV1SearchParams,
  options?: RequestInit
): Promise<getApiV1SearchResponse> => {
  return customFetchAppCatalogo<getApiV1SearchResponse>(
    getGetApiV1SearchUrl(params),
    {
      ...options,
      method: 'GET',
    }
  )
}
